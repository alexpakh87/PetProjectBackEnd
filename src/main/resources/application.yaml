spring:
  datasource:
    name: PetProjectBackend
    url: jdbc:postgresql://localhost:5434/pet
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate

#  flyway:
#    baseline-on-migrate: false

